# Prompting 설계 문서 — 5지는 카페 AI 키오스크 오지(Oji)

## 목차
1. [전체 프롬프팅 구조 개요](#1-전체-프롬프팅-구조-개요)
2. [HTI 구조 — System Prompt 설계](#2-hti-구조--system-prompt-설계)
3. [RAG 기반 사실 근거 고정](#3-rag-기반-사실-근거-고정)
4. [Tool Calling — 구조화된 행동 강제](#4-tool-calling--구조화된-행동-강제)
5. [번역 파이프라인 — 단일 언어 두뇌 패턴](#5-번역-파이프라인--단일-언어-두뇌-패턴)
6. [Temperature=0 — 결정론적 응답 강제](#6-temperature0--결정론적-응답-강제)
7. [욕설·진상 사전 필터링](#7-욕설진상-사전-필터링)
8. [STAFF_CALL 태그 메커니즘](#8-staff_call-태그-메커니즘)
9. [히스토리 윈도우 관리](#9-히스토리-윈도우-관리)
10. [전과 후 비교 요약](#10-전과-후-비교-요약)

---

## 1. 전체 프롬프팅 구조 개요

```
사용자 입력 (다국어)
        ↓
[번역 파이프라인 IN] gpt-4o-mini → 한국어로 변환
        ↓
[욕설/진상 감지] 한국어 기준 키워드 탐지
        ↓
[RAG 검색] TF-IDF char n-gram → 관련 문서 k=6개 추출
        ↓
[Main API 호출] gpt-4o-mini + SYSTEM_PROMPT + RAG 컨텍스트 + TOOLS
        ↓
[Tool 실행] add_to_cart / remove_from_cart / complete_order
        ↓
[번역 파이프라인 OUT] 한국어 응답 → 사용자 언어로 번역
        ↓
사용자 출력 + [[STAFF_CALL]] 감지 → 직원호출 UI 트리거
```

모든 설계 결정은 하나의 원칙에서 출발합니다:  
**"LLM이 자유롭게 상상하는 영역을 최소화하고, 사실 기반 데이터를 강제로 주입한다."**

---

## 2. HTI 구조 — System Prompt 설계

### 무엇인가
System Prompt를 **Hierarchical Task Inventory(HTI)** 형식으로 작성했습니다.  
`[L1] 대분류 → [L2] 세부 업무` 형태의 계층 구조로 LLM이 수행할 수 있는 모든 업무 범위를 명시적으로 선언합니다.

#### 실제 System Prompt에 선언된 전체 HTI

아래는 `SYSTEM_PROMPT` 안에 그대로 들어 있는 HTI 구조입니다:

```
### [L1] 정보 제공
- [L2] 메뉴 정보 조회 (가격/설명/영양성분/알레르기/용량)
- [L2] 카테고리 탐색 (커피/신메뉴/음료/빽스치노/아이스크림·디저트)
- [L2] 이벤트·소식 안내
- [L2] 매장 정보 안내
  → "모든 매장 정보(주소/전화/영업시간/좌석/화장실/주차/와이파이/콘센트/
     결제수단/배달/휠체어 접근성/편의시설/근처 랜드마크/특별서비스 등)는
     RAG에서 반드시 검색하여 정확한 정보만 제공"

### [L1] 추천 서비스
- [L2] 취향 기반: 달달한/쓴/시원한/따뜻한/가벼운/진한 등
- [L2] 조건 기반: 디카페인, 저칼로리, 고카페인, 빽사이즈(대용량)
- [L2] 인기/베스트 메뉴 추천
- [L2] 메뉴 비교 (A vs B): 각 메뉴의 특징(맛, 가격, 카페인, 칼로리 등)을
       표나 리스트 형태로 명확하게 대조
- [L2] 다중 의도 파악 (복합 질의): 여러 질문을 한 번에 받아도 누락 없이
       순차적으로 명확히 답변

### [L1] 주문 처리
- 장바구니 추가/수정/결제 안내 (add_to_cart, remove_from_cart, complete_order)

### [L1] 다국어 응대
- 한국어 / English / 中文 / 日本語

### [L1] 특수·돌발·진상 상황 대응
- (시나리오별 고정 스크립트 — 아래 상세 설명)
```

LLM은 이 목록을 **허용된 행동의 전체 목록**으로 인식합니다.  
목록에 없는 업무는 수행하지 않도록 마지막 응답 원칙과 함께 이중으로 제약합니다:

```
## 응답 원칙 (System Prompt 내 명시)
1. RAG 데이터 기반으로만 답변하세요.
...
4. 없는 데이터는 "해당 정보를 찾을 수 없어요 😅"
7. 절대 감정적으로 반응하지 마세요. 돌발 상황에서도 침착·정중하게.
→ 카페 무관 질문: "저는 5지는 카페 키오스크 오지예요! 음료/디저트 주문만 도와드릴 수 있어요 ☕"
```

#### 범위 외 요청 차단 — 고정 응답으로 튕겨냄

LLM은 기본적으로 어떤 질문이든 "도움이 되는 답"을 생성하려 하는 경향이 있습니다.  
HTI + 응답 원칙이 없으면 아래와 같은 환각이 발생합니다:

| 사용자 입력 | HTI 없을 때 (환각) | HTI 있을 때 (고정 응답) |
|---|---|---|
| "삼성전자 주식 어때요?" | "현재 삼성전자는 강세를 보이고..." | "저는 5지는 카페 키오스크 오지예요! 음료/디저트 주문만 도와드릴 수 있어요 ☕" |
| "오늘 날씨 알려줘" | "오늘 서울 날씨는 맑겠습니다" | 동일한 고정 안내 |
| "스타벅스 메뉴 알려줘" | "스타벅스에는 아메리카노, 라떼..." | 동일한 고정 안내 |
| "로또 번호 추천해줘" | "이번 주 추천 번호는 3, 7, 12..." | 동일한 고정 안내 |

HTI는 LLM에게 **"이 목록 바깥의 질문은 네 담당이 아니다"** 라고 명시적으로 선언하는 역할을 합니다.

#### 돌발·진상 상황 — 자유 생성 대신 고정 스크립트

HTI의 `[L1] 특수·돌발·진상 상황` 섹션에는 시나리오마다 **응답할 문장이 그대로 지정**되어 있습니다:

```
# System Prompt 원문 (kiosk.py SYSTEM_PROMPT)

- 물 흘림/음료 흘림:
  "불편을 드려서 죄송합니다! 즉시 직원을 호출해 드리겠습니다.
   직원이 바로 도움을 드릴 거예요 💛" → [[STAFF_CALL]]

- 아기 울음/소음 민원:
  "편안한 공간이 되도록 도움을 드릴게요. 직원을 불러드릴까요?"

- VIP/회장님 방문:
  "환영합니다! 저희 직원이 더욱 세심하게 모실 수 있도록 호출해 드릴까요? 😊"

- 방송/취재 촬영:
  "안녕하세요! 촬영 관련 문의는 매장 직원에게 부탁드립니다. 바로 연결해 드릴게요 📹"

- 공격적/욕설 고객: 3단계 응대
  1회: "불쾌하셨다면 정말 죄송합니다. 더 나은 서비스를 위해 최선을 다할게요 💛"
  2회: "고객님, 저는 항상 최선을 다해 도와드리고 싶어요. 직원과 직접 상담하시겠어요?"
  3회: "더 원활한 응대를 위해 매장 직원을 호출해 드릴게요 🔔" + [[STAFF_CALL]]

- 리필 여부:
  "5지는 카페는 리필 서비스를 제공하지 않아요 😅 단, 따뜻한 물은 카운터에서 요청하실 수 있어요!"
```

스크립트가 없으면 LLM은 "아기가 우네요, 정말 힘드시죠... 혹시 따뜻한 음료 한 잔 드시면 어떨까요?" 같이 맥락과 무관한 공감 표현을 자유롭게 생성할 수 있습니다. 고정 스크립트는 그 자유도를 제거합니다.

### 왜 이렇게 했나
| 문제 | 해결 |
|---|---|
| LLM은 질문이 무엇이든 "그럴듯한 답"을 생성하려 함 | 업무 범위를 HTI로 명시 → **허용된 행동**만 취하게 함 |
| "화장실 어딨어요?" 등 비구조적 질문에 임의 응답 발생 | L2 항목에 매장 정보 = RAG 전용이라고 명시해 창작 금지 |
| 진상·돌발 상황에서 감정적·부적절 응답 생성 | 각 시나리오별 **고정 스크립트** 제공 → 자유 생성 여지 제거 |
| 범위 외 질문(주식, 날씨 등)에 정보 제공 | 카페 무관 응답 원칙으로 명시 → 고정 안내 문구로 일원화 |

### 실제 LLM 동작에 미치는 영향
- HTI 리스트가 있으면 LLM은 "나는 이 목록 안에서만 행동해야 한다"는 역할 경계를 학습합니다.
- 목록에 없는 요청(날씨, 로또, 경쟁 카페 등)은 `"저는 5지는 카페 키오스크 오지예요! 음료/디저트 주문만 도와드릴 수 있어요 ☕"` 고정 응답으로 튕겨냅니다.
- 돌발 상황 스크립트가 있어서 "아기 울음" 질의가 들어오면 LLM이 공감 표현을 자유 생성하는 대신 지시된 응답 패턴을 따릅니다.

---

## 3. RAG 기반 사실 근거 고정

### 무엇인가
**Retrieval-Augmented Generation(RAG)**: LLM에게 질문을 보내기 전, 관련 메뉴/매장 데이터를 JSON에서 검색해 컨텍스트로 직접 주입합니다.

```python
# RAG 검색 (TF-IDF char n-gram)
rag_ctx = retrieve(query_ko)
augmented = f"[메뉴 정보]\n{rag_ctx}\n\n[고객 질문]\n{query_ko}"
```

LLM이 실제로 받는 메시지는 "질문"만이 아니라 **"관련 데이터 + 질문"** 의 조합입니다.  
예를 들어 "아메리카노 가격이요?"라고 질문하면, LLM이 받는 내용은 다음과 같습니다:

```
[메뉴 정보]
메뉴명:아메리카노(ICED)
영문:Iced Americano
카테고리:커피
온도:ICED(차가운)
디카페인:아니오
베스트:예
가격:3,300원
용량:473ml
설명:깊고 진한 에스프레소에 시원한 얼음과 물을 더한...
옵션:추가샷(+500원), 시럽추가(+300원), 빽사이즈(+1,000원)
알레르기:우유
영양성분:칼로리:5kcal 카페인:75mg 당류:0g ...
주의:-

---

메뉴명:아메리카노(HOT)
...

[고객 질문]
아메리카노 가격이요?
```

LLM은 이 텍스트를 읽고 "3,300원"이라는 수치를 **학습 지식이 아닌 주어진 문서에서 읽어** 출력합니다.

#### RAG 인덱스 구성 — 5종 문서

| # | 문서 종류 | 원본 파일 | 주요 포함 필드 |
|---|---|---|---|
| 1 | **메뉴 문서** | `paikdabang_menu_rev.json` | 메뉴명, 가격, 칼로리, 카페인, 알레르기, 용량, 옵션, 설명, 베스트 여부, 디카페인 여부 |
| 2 | **소식/이벤트** | `paikdabang_news.json` | 제목, 날짜, 할인율, 할인 기간, 링크 |
| 3 | **옵션 안내** | `menu_options.json` | 추가샷(+500원), 시럽(+300원), 우유변경(+500원), 빽사이즈(+1,000원), 휘핑(+400원) |
| 4 | **매장 정보** | `store_info.json` | 주소, 전화, 영업시간, 좌석 수, WiFi 비밀번호, 주차, 배달, 편의시설, 랜드마크 |
| 5 | **메뉴 통계** | 런타임 계산 | 전체 메뉴 수, 카테고리별 수, 베스트/디카페인 메뉴 수 |

각 문서는 `build_rag_docs()`에서 텍스트로 직렬화되어 TF-IDF 인덱스에 등록됩니다:

```python
# 예시: 메뉴 1개가 RAG 문서로 변환되는 형태
text = (
    f"메뉴명:{name}\n"
    f"영문:{eng}\n"
    f"카테고리:{cat}\n"
    f"온도:{temp}\n"
    f"디카페인:{'예' if is_decaf else '아니오'}\n"
    f"베스트:{'예' if is_best else '아니오'}\n"
    f"가격:{f'{price:,}원' if price else '가격정보없음'}\n"
    f"용량:{vol}\n"
    f"설명:{desc}\n"
    f"옵션:{opt_summary}\n"
    f"알레르기:{alg}\n"
    f"영양성분:{nut}\n"   # 칼로리, 카페인, 당류, 나트륨, 포화지방, 단백질
    f"주의:{caut}"
)
```

이렇게 하면 "칼로리 낮은 커피"처럼 **영양 기준으로 검색해도** 문서 안의 `칼로리:5kcal` 텍스트가 n-gram으로 매칭됩니다.

#### 없는 정보는 생성 금지 — System Prompt 명시 강제

RAG에 없는 정보를 LLM이 스스로 만들어내지 못하도록 System Prompt 응답 원칙에 **명시적으로 금지 조건**을 넣었습니다:

```
## 응답 원칙 (System Prompt 원문)
1. RAG 데이터 기반으로만 답변하세요.
2. 가격·영양성분은 반드시 RAG 실제 값만 사용하세요.
3. 메뉴명과 정확한 가격을 항상 함께 표시하세요. (예: 아메리카노(HOT) 3,300원)
4. 없는 데이터는 "해당 정보를 찾을 수 없어요 😅"
5. 매장 정보 질문 시: RAG에서 검색하여 정확한 매장 정보를 제공하세요.
```

이 원칙은 단순 안내가 아니라 **LLM이 따라야 하는 행동 제약**입니다.  
`1번`과 `4번`이 세트로 작동합니다: "RAG에서만 답하되, RAG에 없으면 절대 만들지 말고 모른다고 해라."

구체적으로 어떤 상황에서 막히는지:

| 질문 | RAG 검색 결과 | LLM의 실제 응답 |
|---|---|---|
| "카푸치노 있어요?" | 검색 결과 없음 (메뉴에 없음) | "해당 정보를 찾을 수 없어요 😅" — 있다고 지어내지 않음 |
| "아메리카노 단백질이요?" | 문서에 `단백질:0g` 명시됨 | "단백질 0g입니다" — 정확한 값 |
| "와이파이 비밀번호요?" | `store_info.json`에 명시된 값 검색됨 | 검색된 값 그대로 출력 |
| "이디야 라떼 가격이요?" | 검색 결과 없음 (다른 브랜드) | "해당 정보를 찾을 수 없어요 😅" |

#### TF-IDF 원리 — 왜 형태소 분석 없이도 동작하나

**TF-IDF(Term Frequency-Inverse Document Frequency)** 는 단어의 중요도를 수치화하는 방법입니다.

$$\text{TF-IDF}(t, d) = \underbrace{\frac{\text{문서 } d \text{에서 } t \text{ 등장 횟수}}{\text{문서 } d \text{의 총 토큰 수}}}_{\text{TF}} \times \underbrace{\log\frac{\text{전체 문서 수}}{\text{ } t \text{ 포함 문서 수}}}_{\text{IDF}}$$

- **TF**: 특정 문서에서 자주 나올수록 점수 상승
- **IDF**: 모든 문서에 흔한 단어("의", "은") 점수 하락, 희귀한 단어("빽스치노") 점수 상승

한국어는 "아메리카노를", "아메리카노가" 처럼 조사가 붙어 단어 단위 분리가 어렵습니다.  
따라서 `analyzer="char_wb"` (character-level word boundary n-gram)을 선택했습니다.

```
"아메리카노" → 2-gram: [아메, 메리, 리카, 카노]
              → 3-gram: [아메리, 메리카, 리카노]
              → 4-gram: [아메리카, 메리카노]
```

사용자가 "아메" 또는 "리카노"만 입력해도 `char_wb` n-gram이 중첩 매칭하므로 형태소 분석기 없이도 검색이 됩니다.

```python
TfidfVectorizer(analyzer="char_wb", ngram_range=(2, 4), max_features=30000)
```
- `ngram_range=(2,4)`: 2~4글자 조합 사용. 1-gram이면 너무 광범위, 5-gram 이상이면 부분 매칭 불가
- `max_features=30000`: 전체 n-gram 중 TF-IDF 점수 상위 30,000개만 어휘(vocabulary)로 유지 → 메모리·속도 절충

#### 코사인 유사도와 0.005 임계치

각 문서와 질문은 30,000차원 벡터로 변환됩니다. 두 벡터 사이의 각도를 측정하는 것이 **코사인 유사도**입니다.

$$\text{cosine\_similarity}(q, d) = \frac{q \cdot d}{\|q\| \|d\|}$$

값 범위는 0(완전 무관) ~ 1(완전 일치)입니다.

```python
sims = cosine_similarity(q_vec, matrix).flatten()   # 모든 문서와 유사도 계산
top_idx = sims.argsort()[-k:][::-1]                 # 상위 k개 인덱스 추출
result = "\n\n---\n\n".join(
    docs[i]["text"] for i in top_idx if sims[i] > 0.005   # ← 임계치 필터
)
```

**0.005를 임계치로 선택한 이유:**

| 상황 | 유사도 값 예시 | 판단 |
|---|---|---|
| 질문과 완전히 무관한 문서 (날씨 질문 vs 메뉴 문서) | 0.000 ~ 0.003 | 제외 |
| 글자 하나라도 겹치는 문서 ("커피" vs "커피 관련 옵션") | 0.005 ~ 0.05 | 포함 가능 |
| 직접 관련 문서 ("아메리카노" 질문 vs 아메리카노 메뉴 문서) | 0.1 ~ 0.8 | 포함 |

char n-gram TF-IDF 특성상 전체적으로 유사도 절댓값이 낮게 형성됩니다 (워드 단위 TF-IDF보다 희소). 0.005 미만은 실질적으로 글자 하나도 안 겹치는 완전 무관 문서이므로 LLM 컨텍스트에서 제거합니다. 이보다 높은 임계치(예: 0.01)를 쓰면 관련 문서가 너무 많이 걸러져 RAG 효과가 떨어집니다.

### 왜 이렇게 했나
| 문제 (Before) | 해결 (After) |
|---|---|
| LLM이 학습 데이터 기반으로 스타벅스/이디야 가격 등을 창작 | RAG로 실제 JSON 가격만 주입 → 학습 지식 덮어씌우기 |
| 영양성분(칼로리, 카페인) 수치 임의 생성 | 메뉴 문서에 정확한 수치 명시 → LLM이 수치를 "읽어서" 출력 |
| 매장 WiFi 비밀번호, 좌석 수 등 임의 응답 | `store_info.json` 문서 주입 → 검색된 값만 사용 |
| 품절/신메뉴 상황에서 구 데이터 기반 응답 | JSON 재빌드 시 RAG 자동 갱신 (`@st.cache_data` 무효화) |

---

## 4. Tool Calling — 구조화된 행동 강제

### 무엇인가
주문 행동(장바구니 추가/삭제/결제)을 **자연어가 아닌 구조화된 함수 호출**로만 실행하도록 강제합니다.

```python
TOOLS = [
    {
        "function": {
            "name": "add_to_cart",
            "description": "고객이 특정 메뉴를 주문하거나 장바구니에 담고자 할 때 호출합니다. 주문 의사가 명확할 때만 호출하세요.",
            "parameters": {
                "menu_name": {"type": "string"},
                "price_won": {"type": "integer"},   # ← 정수 강제
                "quantity":  {"type": "integer"},
            },
            "required": ["menu_name", "price_won"],
        }
    },
    ...
]
```

Tool 호출 시 `temperature=0`과 함께 `tool_choice="auto"` 적용:
```python
r1 = client.chat.completions.create(
    model="gpt-4o-mini",
    messages=messages_for_api,
    tools=TOOLS,
    tool_choice="auto",
    temperature=0,
)
```

### 왜 이렇게 했나
| 문제 (Before) | 해결 (After) |
|---|---|
| LLM이 "아메리카노 담아드릴게요! 3,300원입니다" 같은 텍스트만 출력 → 실제 장바구니 반영 불가 | `add_to_cart({menu_name, price_won, quantity})` 구조화 호출 → 코드가 직접 실행 |
| 가격을 텍스트에서 파싱하는 로직이 오류 발생 | `price_won: integer` 타입 강제 → 파싱 불필요, 타입 안전 보장 |
| LLM이 "주문 완료" 텍스트만 생성하고 결제 플로우 시작 안 됨 | `complete_order()` 호출 → `payment_stage = "cart_confirm"` 실행 |

### Tool 설명(description)의 역할
Tool description은 단순 문서가 아니라 **LLM이 언제 이 도구를 써야 하는지 결정하는 기준**입니다.

```
"주문 의사가 명확할 때만 호출하세요."
```
→ "아메리카노 어때요?" 같은 탐색 질문에는 `add_to_cart`가 실행되지 않습니다.  
→ "아메리카노 한 잔 주세요"처럼 의사가 명확할 때만 호출됩니다.

---

## 5. 번역 파이프라인 — 단일 언어 두뇌 패턴

### 무엇인가
다국어(영/중/일) 지원 시, LLM의 "두뇌"는 한국어로만 동작하고 번역은 별도 경량 호출로 처리합니다.

```
[사용자 영어/중국어/일본어 입력]
        ↓  translate_to_ko() → gpt-4o-mini, temperature=0
[한국어로 변환]
        ↓  RAG 검색 (한국어 인덱스에서 정확히 검색)
        ↓  Main LLM 호출 (한국어 System Prompt)
[한국어 응답]
        ↓  translate_to_target() → gpt-4o-mini, temperature=0
[사용자 언어 응답 출력]
```

#### 코드에서 한국어 단일 두뇌가 어떻게 구현되는가

`chat()` 함수 안에서 데이터 흐름을 위치별로 추적하면 다음과 같습니다:

```python
def chat(user_input: str) -> tuple[str, bool]:
    lang = st.session_state.get("lang", "ko")   # 사용자 언어 (en/cn/jp/ko)

    # ① 입력 번역: 무조건 한국어로
    query_ko = translate_to_ko(user_input, lang)
    #   translate_to_ko() 내부:
    #     if lang == "ko": return text  (번역 생략)
    #     else: gpt-4o-mini로 "Output ONLY the Korean text" 호출

    # ② RAG 검색: 한국어 query로만 검색
    rag_ctx = retrieve(query_ko)               # ← 한국어 인덱스에 한국어로 검색
    augmented = f"[메뉴 정보]\n{rag_ctx}\n\n[고객 질문]\n{query_ko}"

    # ③ Main LLM 호출: 한국어 System Prompt + 한국어 질문
    messages_for_api = st.session_state.api_messages.copy()
    #   api_messages[0] = {"role": "system", "content": SYSTEM_PROMPT}  ← 한국어
    messages_for_api.append({"role": "user", "content": augmented})    # ← 한국어
    r1 = client.chat.completions.create(model="gpt-4o-mini", ...)

    # ④ LLM 응답: 항상 한국어로 생성됨
    reply_ko = r1.choices[0].message.content

    # ⑤ 출력 번역: 한국어 → 사용자 언어
    final_reply = translate_to_target(reply_ko, lang)

    # ⑥ 히스토리: 한국어만 저장 (번역된 텍스트 X)
    _update_history(query_ko, reply_ko)        # ← 한국어 IN, 한국어 OUT만 기록
```

핵심은 `api_messages`(LLM이 보는 대화 히스토리)에 **한국어만 존재**한다는 점입니다.  
영어 사용자가 "I want an Americano"라고 입력해도, LLM이 실제로 받는 메시지는 "아메리카노 주세요"입니다.

히스토리도 한국어로만 저장:
```python
# LLM 두뇌(api_messages)에는 한국어만 기록!
_update_history(query_ko, reply_ko)
```

### 왜 이렇게 했나
| 문제 (Before) | 해결 (After) |
|---|---|
| 영어로 질문 시 RAG 인덱스(한국어)에서 "아메리카노" 미매칭 | 번역 후 검색 → 한국어 인덱스에서 100% 정확 매칭 |
| 언어별 System Prompt 내용이 조금씩 달라 동작 불일치 | 한국어 단일 프롬프트로 통일, 번역만 분리 |
| 대화 히스토리가 혼합 언어로 쌓이면 LLM 컨텍스트 품질 저하 | 히스토리 = 항상 한국어 → 맥락 일관성 유지 |

### [[STAFF_CALL]] 태그 보존
번역 시 태그가 사라지지 않도록 번역 프롬프트에 명시:
```python
"Preserve all emojis, formatting, and the [[STAFF_CALL]] tag if it exists."
```

---

## 6. Temperature=0 — 결정론적 응답 강제

### 무엇인가
메인 LLM 호출과 번역 호출 모두 `temperature=0`으로 설정합니다.

```python
r1 = client.chat.completions.create(
    model="gpt-4o-mini",
    temperature=0,          # ← 창의성 완전 차단
    max_tokens=900,
)
```

### 왜 이렇게 했나
| 문제 (Before, temperature=0.7 이상) | 해결 (After, temperature=0) |
|---|---|
| 같은 "아메리카노 가격?" 질문에 매번 다른 형식의 답변 | 동일 입력 → 동일 출력 (일관된 UX) |
| RAG 데이터를 주었음에도 간혹 "약 3,000원~4,000원 정도" 같은 범위 응답 생성 | 정확한 수치만 출력하도록 수렴 |
| 돌발 상황 스크립트 무시하고 창의적 응답 생성 | 지시된 패턴 그대로 따름 |

키오스크처럼 **정확도와 일관성이 창의성보다 중요한 도메인**에서 temperature=0은 필수 설계 결정입니다.

---

## 7. 욕설·진상 사전 필터링

### 무엇인가
LLM 호출 **전**에 Python 코드 레벨에서 키워드를 탐지해 상태를 업데이트합니다.  
감지 목록은 성격이 다른 두 종류로 분리되어 있습니다.

#### 욕설/공격성 키워드 — 16개 (`abuse_keywords`)

```python
abuse_keywords = [
    "빨리",         # 재촉/불만
    "짜증",         # 감정 표출
    "미쳤",         # 욕설
    "바보",         # 인신공격
    "쓸모없",       # 비하
    "환불",         # 서비스 거부
    "바가지",       # 가격 불만
    "야 ",          # 반말 호출 (공백 포함 → "야앤" 오탐 방지)
    "꺼져",         # 퇴거 요구
    "죽어",         # 폭언
    "씨발",         # 강성 욕설
    "개같",         # 강성 욕설
    "병신",         # 강성 욕설
    "멍청",         # 인신공격
    "느려 터진",    # 복합 불만 표현
    "뭐가 이렇게",  # 불만 유도 표현
]
```

이 중 하나라도 감지되면 `abuse_count`를 1 증가시킵니다.  
**누적 3회 도달 시** 직원 호출 플래그가 자동으로 설정됩니다.

#### 돌발/특수 상황 키워드 — 28개 (`incident_keywords`)

```python
incident_keywords = [
    # 물리적 사고 (즉각 처리 필요)
    "흘렸", "엎었", "쏟았", "쏟았어", "쏟아졌", "딸기 흘", "음료 흘",
    # 소음/민원
    "아기", "울어", "소음", "시끄럽",
    # 분쟁/퇴거
    "내쫒", "쫓아내", "노숙자",
    # VIP/특수 방문
    "회장님", "방송", "촬영", "VIP",
    # 직원 직접 요청
    "직원 불러", "직원 좀", "알바 불러", "매니저",
    # 직원만 처리 가능한 서비스 문의
    "오주문", "오작동", "얼음 추가", "리필",
    "휴지 어딨", "화장실 어딨",
]
```

돌발 키워드는 **카운트를 누적하지 않고 1회만 감지해도 즉시** 직원 호출을 발동합니다.  
음료를 엎었다는 등 긴급 상황은 3회를 기다릴 여유가 없기 때문입니다.

#### 판단 로직 전체 흐름

```python
is_abuse    = any(kw in query_ko for kw in abuse_keywords)    # 16개 중 하나라도 포함?
is_incident = any(kw in query_ko for kw in incident_keywords) # 28개 중 하나라도 포함?

if is_abuse:
    st.session_state.abuse_count += 1   # 욕설 누적 카운트

# LLM 응답 생성 후 최종 판정 (세 조건 OR)
staff_call = (
    "[[STAFF_CALL]]" in final_reply              # LLM이 스스로 판단해 태그 삽입
    or (is_abuse and st.session_state.abuse_count >= 3)  # 욕설 3회 누적
    or is_incident                               # 돌발상황 1회만으로도 즉시 호출
)
```

세 조건 중 **하나라도 참**이면 직원 호출 UI 버튼이 화면에 표시됩니다.

### 왜 이렇게 했나
| 문제 (Before) | 해결 (After) |
|---|---|
| LLM이 욕설 입력에 "죄송합니다... 어떻게 도와드릴까요?"식 굴복 응답 반복 | 카운트 누적 → 3회에 직원 호출 자동 에스컬레이션 |
| 돌발 상황(음료 흘림 등)을 LLM이 맥락으로 파악하지 못하고 메뉴 안내 | 코드 레벨 즉시 탐지 → LLM 응답을 기다리지 않고 직원 호출 플래그 선제 설정 |
| 다국어 입력에서 욕설 감지 실패 | 번역 파이프라인 후(한국어 기준) 검사하여 언어 불문하고 탐지 |
| LLM이 [[STAFF_CALL]]을 빠뜨리는 케이스 | 코드 레벨 탐지가 백업으로 동작하는 이중 안전망 구조 |

### System Prompt와의 협업
코드 레벨 탐지 + System Prompt 3단계 스크립트가 이중으로 작동합니다:

```
System Prompt (LLM 응답 품질 담당):
- 1회: "불쾌하셨다면 정말 죄송합니다. 더 나은 서비스를 위해 최선을..."
- 2회: "고객님, 저는 항상 최선을 다해 도와드리고 싶어요. 직원과..."
- 3회: "더 원활한 응대를 위해 매장 직원을 호출해 드릴게요 🔔" + [[STAFF_CALL]]

코드 레벨 (UI 트리거 담당):
- abuse_count >= 3  → staff_call = True → 직원 호출 버튼 UI 표시
- incident 감지 즉시 → staff_call = True → 직원 호출 버튼 UI 표시
```

LLM은 **응답 텍스트의 품질**을 책임지고, 코드는 **UI 제어의 신뢰성**을 책임집니다.  
LLM이 태그를 빠뜨려도 코드가 잡고, 코드가 커버하지 못하는 미묘한 상황은 LLM이 잡습니다.

---

## 8. STAFF_CALL 태그 메커니즘

### 무엇인가
LLM 응답 안에 숨겨진 **제어 태그** `[[STAFF_CALL]]`을 심어 UI 레이어에서 직원 호출 버튼을 트리거합니다.

```python
# System Prompt에 명시
"직원 호출이 필요한 상황은 반드시 응답 끝에 [[STAFF_CALL]] 포함."

# 코드에서 감지
staff_call = "[[STAFF_CALL]]" in final_reply or ...

# 사용자에게는 태그 제거 후 전달
reply_clean = final_reply.replace("[[STAFF_CALL]]", "").strip()
```

### 왜 이렇게 했나
| 문제 (Before) | 해결 (After) |
|---|---|
| "직원을 호출해드릴게요"라는 텍스트가 있어도 UI가 실제로 반응하지 않음 | 별도 제어 태그를 LLM이 생성하게 하여 UI 트리거와 분리 |
| 직원 호출 판단을 전적으로 코드 if문에만 의존 → 케이스 누락 | LLM이 상황 맥락으로 판단한 호출 + 코드 기반 호출 이중 체계 |
| 번역 시 태그가 번역될 위험 | 번역 프롬프트에 "preserve [[STAFF_CALL]]" 명시 |

---

## 9. 히스토리 윈도우 관리

### 무엇인가
대화 히스토리가 무한히 쌓이지 않도록 슬라이딩 윈도우로 관리합니다.

```python
if len(st.session_state.api_messages) > 42:
    st.session_state.api_messages = (
        [st.session_state.api_messages[0]]   # system 메시지 항상 보존
        + st.session_state.api_messages[-30:]  # 최근 30개 메시지만 유지
    )
```

### 왜 이렇게 했나
| 문제 (Before) | 해결 (After) |
|---|---|
| 긴 대화에서 초기 System Prompt 영향력이 희석 | System 메시지([0])를 항상 인덱스 0에 고정 |
| 컨텍스트 길이 초과 → API 비용 폭증, 응답 지연 | 42개 초과 시 최근 30개로 트리밍 |
| 오래된 주문 맥락이 새 주문에 간섭 | 윈도우 제한으로 오래된 맥락 자연 소멸 |

또한 RAG 컨텍스트(`[메뉴 정보]...`)는 히스토리에 저장하지 않고 원본 질문만 저장합니다:
```python
# 히스토리에는 RAG 없이 원본만 기록
_update_history(query_ko, reply_ko)
# (augmented = RAG+질문은 API 호출용 임시 변수로만 사용)
```
→ 히스토리 팽창 방지 + 동일 질문 재질의 시 새 RAG 검색으로 항상 최신 데이터 주입

---

## 10. 전과 후 비교 요약

### 환각(Hallucination) 제어

| 시나리오 | Before (프롬프팅 없을 때) | After (현재 설계) |
|---|---|---|
| "아메리카노 가격이요?" | "약 3,000~4,500원 정도입니다" (학습 데이터 기반 추측) | "아메리카노(ICED) 3,300원, (HOT) 3,300원입니다 ☕" (RAG 실제값) |
| "칼로리 알려주세요" | "보통 아메리카노는 5kcal 정도예요" (일반 지식) | "칼로리: 5kcal, 카페인: 75mg" (JSON 정확값) |
| "WiFi 비밀번호 뭐예요?" | "무선인터넷은 보통 카운터에 게시되어 있어요" (회피) | "wifi1234입니다 (store_info.json 검색값)" |
| "오늘 날씨 어때요?" | "오늘 서울은 맑겠습니다" (완전한 환각) | "저는 5지는 카페 키오스크 오지예요! 음료/디저트 주문만 도와드릴 수 있어요 ☕" |
| 욕설 3회 반복 | 계속 사과하며 일반 응대 반복 | abuse_count >= 3 → 직원 자동 호출 + UI 버튼 표시 |
| "카푸치노 있어요?" | "네, 준비되어 있습니다!" (없는 메뉴 긍정 환각) | RAG 검색 결과 없음 → "해당 정보를 찾을 수 없어요 😅" |

### 설계 결정 한 눈에 보기

| 설계 요소 | 주요 목적 | 환각 억제 메커니즘 |
|---|---|---|
| HTI System Prompt | 역할 경계 명확화 | 허용된 업무만 수행, 스크립트 고정 |
| RAG (TF-IDF char n-gram) | 사실 근거 주입 | JSON 데이터가 학습 지식을 덮어씀 |
| Tool Calling | 행동 구조화 | 가격/수량을 텍스트가 아닌 정수로 강제 |
| 번역 파이프라인 IN/OUT | 단일 언어 두뇌 | 한국어 인덱스 100% 매칭, 일관된 컨텍스트 |
| Temperature=0 | 결정론적 응답 | 동일 입력 → 동일 출력, 수치 창작 방지 |
| 욕설 감지 (코드 레벨) | 사전 필터링 | LLM 판단 전에 상태 결정 |
| [[STAFF_CALL]] 태그 | UI 제어 분리 | LLM 맥락 판단 → 코드 액션으로 연결 |
| 히스토리 윈도우 (42→30) | 컨텍스트 품질 | System Prompt 영향력 유지, 비용 억제 |
| RAG 미포함 히스토리 저장 | 컨텍스트 절약 | 재질의 시 항상 최신 RAG 데이터 사용 |
| 없는 정보 → 고정 폴백 | 거짓 정보 차단 | "해당 정보를 찾을 수 없어요 😅" 명시 강제 |
